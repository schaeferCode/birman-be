import { CustomerInstance } from './customer';
import { GoogleAdsNodeOptions } from './grpc';
import { ClientOptions, CustomerOptions } from './types';
interface ListAccessibleCustomersOptions extends GoogleAdsNodeOptions {
    refresh_token: string;
}
export default class GoogleAdsApi {
    private readonly options;
    private throttler;
    constructor(options: ClientOptions);
    Customer({ customer_account_id, refresh_token, login_customer_id, pre_report_hook, post_report_hook, prevent_mutations, logging, }: CustomerOptions): CustomerInstance;
    listAccessibleCustomers({ refresh_token, prevent_mutations, logging, }: ListAccessibleCustomersOptions): Promise<Array<string>>;
}
export {};
